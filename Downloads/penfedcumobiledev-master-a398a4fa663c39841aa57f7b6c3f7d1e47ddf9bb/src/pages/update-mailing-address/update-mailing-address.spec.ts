/*import {TestBed} from '@angular/core/testing';
import { FormGroup,  FormBuilder  } from '@angular/forms';
import { IonicModule, Platform, NavController, NavParams, LoadingController } from 'ionic-angular/index';
import { HttpModule, Http } from "@angular/http";
import {UpdateMailingAddressPage} from './update-mailing-address';

describe('Component: UpdateMailingAddressPage',()=>{
    let component: UpdateMailingAddressPage;

    beforeEach(()=>{
        TestBed.configureTestingModule({
            declarations:[UpdateMailingAddressPage],
            imports:[FormBuilder],
            providers:[]
        });
        const fixture = TestBed.createComponent(UpdateMailingAddressPage);
        component = fixture.componentInstance;
    });

     it('should have a defined component', () => {
        expect(component).toBeDefined();
    });
})

*/